pre-commit:
  parallel: true
  jobs:
    - name: "biome check"
      glob: "src/*.{js,ts,jsx,tsx,json,css}"
      run: bun biome:check --no-errors-on-unmatched --colors=off {staged_files}
      stage_fixed: true
      skip:
        - merge
        - rebase
